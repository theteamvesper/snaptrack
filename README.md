# Team Vesper — React + Vite + Tailwind

```bash
npm install
npm run dev
```

Assets live in `public/assets/` and are referenced as `/assets/*`.
The 3180px studio banner is pre-optimised to 1800px (24 KB JPEG) and is only ever
painted as a masked, GPU-composited background layer — never as an <img>.
